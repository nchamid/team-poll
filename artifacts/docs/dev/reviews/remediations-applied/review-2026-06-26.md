# /review remediations — Team Poll — 2026-06-26

Build-completion gate, single iteration. Status: **CLEAN**.

## Mechanical fixes applied

| File | Issue | Severity | Source | Fix | Status |
|---|---|---|---|---|---|
| web/src/.../PickRow.module.css (×2), Avatar, Badge, EmptyState, ErrorState `.module.css` | `border-radius: 50%` on circular elements — off-token radius | High | Design-conformance hook (`check-design-conformance.sh`) | Replaced with `var(--radius-pill)` (identical circle on a square, rule-compliant) | Applied |
| web/.prettierignore (new) | `prettier --check .` flagged generated `dist/` build output | Low | Pre-Impl / web-linting-formatting.md | Added `.prettierignore` excluding `dist/`, `build/`, `coverage/`, `node_modules/`, `*.tsbuildinfo` | Applied |

## Gates

- API: `dotnet build` 0 warnings / 0 errors; `dotnet test` **19/19 passed, 0 skipped** (incl. the 3 DB-enforced vote-uniqueness tests against real SQL Server).
- Web: `eslint` clean; `prettier --check` clean; `tsc --noEmit` clean; `vitest run` **65/65 passed** (vitest-axe on every interactive component).
- Design conformance hook: **PASS** (all colours/radii trace to tokens).
- tSQLt: runs in CI (`database` job); procs also exercised by API integration tests locally.

## Universal guardrails — 9/9 pass

No user input/PII in logs (IDs/counts only); ProblemDetails with plain-language detail (no stack traces); no secrets in source; parameterised stored-proc calls (no SQL concat); no `eval`/`dangerouslySetInnerHTML`; `UseHttpsRedirection`; boundary validation; ownership → 403 not 404; `CancellationToken` threaded throughout.

## Code-review + OWASP A01–A10

No Critical/High/Medium findings. Spot-checks: `[Authorize]` on all controllers (health anon only); `Cache-Control: private, no-store` default; security headers (CSP/nosniff/Referrer/HSTS); CORS scoped to configured origins (no `AllowAnyOrigin`); MSAL `sessionStorage`; secrets in Key Vault; `appsettings.Development.json` holds placeholders only.

## Architectural findings

None. No batched prompt required.
